<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0A0A0A"
    android:padding="24dp"
    android:gravity="center_horizontal">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pixel 8 Dynamic Island"
        android:textColor="#FFFFFF"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginTop="20dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="الجزيرة التفاعلية المتطورة v3.0"
        android:textColor="#38BDF8"
        android:textSize="14sp"
        android:layout_marginBottom="32dp" />

    <!-- Button 1: Overlay Permission -->
    <Button
        android:id="@+id/btnOverlayPermission"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="1. تفعيل الجزيرة فوق التطبيقات ⚡"
        android:backgroundTint="#0284C7"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp" />

    <!-- Button 2: Notification Permission -->
    <Button
        android:id="@+id/btnNotificationPermission"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="2. إذن الإشعارات والموسيقى 🎵"
        android:backgroundTint="#16A34A"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp" />

    <!-- Button 3: Test Dynamic Island -->
    <Button
        android:id="@+id/btnTestIsland"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="3. اختبار الأنشطة الحية فوراً ✨"
        android:backgroundTint="#D97706"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:textStyle="bold" />

</LinearLayout>
